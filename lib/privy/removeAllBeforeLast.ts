import { getAndRemoveAllBeforeLast } from '@writetome51/array-get-and-remove-all-before';


// value cannot be an object.

export function removeAllBeforeLast(value: any, array): void {
	getAndRemoveAllBeforeLast(value, array);
}
